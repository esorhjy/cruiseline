# AI 修改入口

修改本專案前，先閱讀以下文件；以工作區最新內容與使用者最新明確要求為準，不只依賴對話摘要或歷史記憶。

1. `PROJECT_BLUEPRINT.md`：架構、導覽、離線與產品邊界。
2. `CONTENT_RULES.md`：資料來源、資格限制、待確認事項與穩定 ID。
3. `SEARCH_RULES.md`：搜尋排序、中英對照、菜單與 Crew 操作。
4. `docs/TYPOGRAPHY_2026-09-08.md`：中文字級、字型備援與放大驗收。
5. `docs/NOTEBOOK_REDESIGN_2026-09-07.md`：內容搬移與舊連結對照；涉及行程事實另讀 `docs/CONTENT_UPDATE_2026-09-07.md`。
6. `docs/CONTENT_UPDATE_2026-09-08.md`：使用者指定的最新登船附件、原欄位更新對照及菜單修正；相同主題優先於舊資料。

## 不得退回的設計

- 原封面是使用者指定的精神象徵：完整保留主體與比例，不替換、不裁切、不縮成導覽圖示。
- 保留航程／船上探索／行前準備三畫面；菜單直接開搜尋餐點分類，首頁不載入或渲染菜單。
- 字級使用 rem：預設根字級下，正文 17px、操作 16px、輔助至少 14px。不得為第一屏塞更多資料而縮小手機中文字；保留使用者縮放能力。
- 菜單保留完整 snapshot 與同名餐點的各餐廳版本，餐廳優先、段落第二層；lazy load、retry、runtime cache 不可退回同步載入。
- 搜尋與 Crew 保留焦點、篩選及捲動。正常字級下結果區至少占面板 65%；放大後優先讓內容與控制可達。
- 保留既有清單 ID、勾選狀態、資料 ID 與舊連結；不得把建議時段或歷史航次經驗當成正式安排。
- 純靜態、純關鍵字搜尋，不新增 AI、後端或首頁大型重複區塊。

## 修改後

- 依修改範圍執行規則文件中的資料、搜尋與瀏覽器回歸；字級／版面變更必跑 `tests/typography.browser.mjs`，含 200% 字體放大與間距覆寫。
- 核心資產變更同步 build ID 與 Service Worker；部署驗收需核對線上版本、必要資源 HTTP 200，不能只憑 push 成功宣稱上線。
- 明確區分瀏覽器模擬、實機驗證與未驗證項目；不要宣稱完整 WCAG 合規。
- 使用者確認新的長期設計原則時，更新對應規則文件；本檔只作入口，不另建互相矛盾的規格。
